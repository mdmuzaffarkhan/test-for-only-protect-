stages:
  - build
  - status
  
build:
    only:
      changes:
        - kabilan1
    stage: build
    tags:
      - stg
    script:
      - echo "pulled the code"
      - echo "kabilan1"
      - git status
status:
    only:
      - staging
    stage: status
    tags:
      - stg
    script:
      - echo "check service status"
      - systemctl status nginx.service
