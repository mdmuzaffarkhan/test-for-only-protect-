stages:
  - build

kabilan1:
  stage: build
  tags:
    - stg
  script:
    - echo "kabilan1"
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" 
    changes:
      - kabilan1/*

kabilan2:
  stage: build
  tags:
    - stg
  script:
    - echo "kabilan2"
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" 
    changes:
      - kabilan2/*

