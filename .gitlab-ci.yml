stages:
  - build

build:
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule" 
    changes:
      - kabilan1/*
  stage: build
  tags:
    - stg
  script:
    - echo "pulled the code"
    - echo "kabilan1"
    - git status
    
