stages:
  - build

build:
  only: 
    changes:
     - kabilan1/*
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule"
  stage: build
  tags:
    - stg
  script:
    - echo "pulled the code"
    - echo "kabilan1"
    - git status
