stages:
  - build
  - status
  
build:
    only:
      - staging
    stage: build
    tags:
      - stg
    script:
      - echo "pulled the code"
      - pwd
      - git status
status:
    only:
      - staging
    stage: status
    tags:
      - stg
    script:
      - echo "check service status"
      - systemctl status nginx.service
