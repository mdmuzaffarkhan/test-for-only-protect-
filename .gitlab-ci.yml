stages:
  - build
  - Install

build:
  stage: build
  tags:
    - stg
  rules:
  - if: $CI_PIPELINE_SOURCE == "schedule" 
  script:
    - sh /mnt/script2.sh
Install:
  stage: install
  tags:
    - stg
  script:
    - yum info nginx
  when: manual
