stages:
  - build
  - status

build:
    stage: build
    tags:
      - test
    only:
       - master
    script:
      - echo "pull the code"
      - git pull origin master

status:
    stage: status
    tags:
      - test
    script:
      - echo "check service status"
      - systemctl status sdcdemo.service
